import { ContentBlock } from 'draft-js';
export type StrategyCallback = (start: number, end: number) => void;
export declare function findWithRegex(regex: RegExp, contentBlock: ContentBlock, callback: StrategyCallback): void;
